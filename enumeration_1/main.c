#include <stdio.h>
#include <stdlib.h>

//Enumeration is needed for indexing static arrays elements

enum week {Monday = 1, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};

int main() {

    printf("%d", Wednesday);
    return 0;
}
